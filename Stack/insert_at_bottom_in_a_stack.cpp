// Code studio 3

/*

void solve(stack<int> &s, int x)
{

    // Base case
    if (s.empty())
    {
        s.push(x);
        return;
    }

    int num = s.top();
    s.pop();

    // Recursive call
    solve(s, x);

    s.push(num);
}

satck<int> pushAtBottom(stack<int> &myStack, int x)
{
    solve(myStack, x);
    return myStack;
}


*/